--Fazer um validator e applicar logica
--Corrigir o seg fault do retorno ao main menu (solved: o frame não estava sendo destruído)
--Corrigir saida de texto do OCR na tela de detecção(opcional)
--Ainda Falta fazer as outras partes do calendario, depois que ele executar uma executar a outra, tem que fazer validação pra isso na controller.
--fazer um recebimento de lote, não fiz validação pra dias invalidos no calendario, tem que fazer isso também.(parcialmente feito)(lote implementado,fazer validator)