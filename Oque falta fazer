Implementado:
    --Fazer um validator e aplicar logica(aplicado)
    --Corrigir o seg fault do retorno ao main menu (solved: o frame não estava sendo destruído)
    --Corrigir saida de texto do OCR na tela de detecção(opcional)(não necessario)
    --Ainda Falta fazer as outras partes do calendario, depois que ele executar uma executar a outra, tem que fazer validação pra isso na controller.(Feito parcialmente)
    --fazer um recebimento de lote, não fiz validação pra dias invalidos no calendario, tem que fazer isso também.(parcialmente feito)(lote implementado,fazer validator)(validator implementado)
    --fazer um arquiver (implementado)
    --Descobrir oque esta ocasionando o erro de saida (Esta chamando o destrutor duas vezes, é bom manter mesmo  que dê um warning para que tudo seja finalizado caso ocorra algum problema)
    -Implementar GUI em thread separado (Talvez não seja necessário, mas é bom manter a ideia)(ATT: MUITO DIFICIL e não vai mudar no desempenho)
    --Corrigir a raspicam em cpp, estou tentando transferir a libcamera para o video10 mas esta dando muito problema, talvez seja valido procurar outra lib(!!!PRIORIDADE!!!)
    (A principio resolvido)
    --Atualizar diagrama (fazer um de sequencia)(Parcial)
    --Melhorar desempenho (Eu otimizei um pouco mas por conta do tamanho da imagem e da necessidade de conversão está consumindo muito recurso)(A finalizar)
    --Era interessante fazer um yml de configuração geral mas não é prioridade e vai ser trabalhoso.Fica a criterio posteriormente.(Arquiver melhorado)
    --Camera trava após algum tempo coletando dados da camera (pelo oque eu vi pode ser problema de buffer,posso ter resolvido com a alteração da pipeline, mas não tenho certeza)
    --Implementar a GUI para seleção da quantidade de impressões, acho  que pode ser tipo um combo mas não tenho certeza.(Feito)
    --Implementar o objeto da impressora, a seleção dos dados está pronta, tem que definir no contrutor e fazer os getters e setters para encapsular,
    além disso tem que testar em c++  e o quão nativo é o driver raw, se terá que configurar de tempos em tempos ou como fica.(A principio funciona diretamente sem ter que fazer uma reconfiguração
    mas é bom testar mais, até então foi necessario apenas uma confuguração e tive problema com o driver poucas vezes)


Falta colocar na Controller:
    --implementar sensors(Falta colocar na controller, mas tem que fazer de forma a não travar a GUI ou implementar direto na detector)
    --implementar out strobo('Falta colocar na controller mas a principio está implementado')

A fazer/Terminar:
    --Camera atrasada, (o frame rate não está ruim mas a camera está demorando para exibir a imagem é possivel que eu tenha resolvido qundo alterei a pipeline)

Ultimas atualizações:
    --Fiz o botão do shutdown e fiz o lock da screen, o sistema agora é fechado, é interessante considerar de fazer um catch que finalize o programa caso algo ocorra.