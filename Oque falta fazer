--Fazer um validator e aplicar logica(aplicado)
--Corrigir o seg fault do retorno ao main menu (solved: o frame não estava sendo destruído)
--Corrigir saida de texto do OCR na tela de detecção(opcional)(não necessario)
--Ainda Falta fazer as outras partes do calendario, depois que ele executar uma executar a outra, tem que fazer validação pra isso na controller.(Feito parcialmente)
--fazer um recebimento de lote, não fiz validação pra dias invalidos no calendario, tem que fazer isso também.(parcialmente feito)(lote implementado,fazer validator)(validator implementado)
--fazer um arquiver (implementado)
--Descobrir oque esta ocasionando o erro de saida (Esta chamando o destrutor duas vezes, é bom manter mesmo  que dê um warning para que tudo seja finalizado caso ocorra algum problema)
-Implementar GUI em thread separado (Talvez não seja necessário, mas é bom manter a ideia)

--implementar sensors(Falta colocar na controller, mas tem que fazer de forma a não travar a GUI ou implementar direto na detector)
--implementar out strobo('')
--Atualizar diagrama (fazer um de sequencia)(Parcial)
--Corrigir a raspicam em cpp, estou tentando transferir a libcamera para o video10 mas esta dando muito problema, talvez seja valido procurar outra lib(!!!PRIORIDADE!!!)
--Melhorar desempenho (EU começei a otimizar mas por conta do tamanho da imagem está ficando bem pesado, tentar otimizar principalmente a interface e a captura)(A finalizar)
--Era interessante fazer um yml de configuração geral mas não é prioridade e vai ser trabalhoso.Fica a criterio posteriormente.