# Cálculo baseado em um sensor que entrega 1280x960 por padrão
# Cortando o centro (320, 240) com tamanho 640x480
pipeline = (
    "libcamerasrc selection-rect=\"<320,240,640,480>\" ! "
    "video/x-raw, width=640, height=480 ! "
    "videoconvert ! "
    "video/x-raw, format=BGR ! "
    "queue max-size-buffers=1 leaky=downstream ! "
    "appsink max-buffers=1 drop=true"
)